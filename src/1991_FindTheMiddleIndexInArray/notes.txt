2026-02-16

why is there a duplicate of this problem?
I think its because they release problems within the
competition thing, and I guess the just forgot they already
done this one?

Got to use all my favorite features pipes (|>) and
pattern matching, look how this is just so much better
the predicate function

fn t -> elem(t, 0) == elem(t, 1) end 

	or

fn {a, b} -> a == b end 

Also, short circuit evaluation
(nil || -1) -> nil
(x   || -1) -> x

Should comeback and do this in golang.
