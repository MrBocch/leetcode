2025-10-31

You could also write a one line solution using
a hashmap in scala, but its just really
ugly considering scala has no built in tally.

im learning uiua, and I asked my self, how would i
solve if had no hashmap only arrays.

(dont know much uiua currently but this is probably how right? a sliding window)

In elixir, is there a difference between calling
Enum.filter(...)
Map.filter(...)

Enum.keys
Map.keys
Just looked at the repl they are different, I think prefer to use
the explicit to Map.filter.

Asked chaptgpt and it said you can also do it like this

def get_sneaky_numbers(nums) do
  for {k, 2} <- Enum.frequencies(nums), do: k
end

WHAT!!! WOW!, was dissapointed to see the lame syntax
for lambda function but wow, that totally makes up for it.
